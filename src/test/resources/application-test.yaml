spring:
  datasource:
    username: postgres
    password: admin123
    driver-class-name: org.postgresql.Driver
  liquibase:
    enabled: false
logging:
  level:
    org.springframework.jdbc.datasource.init: DEBUG